#include <stdio.h>
/**
 * main - Entry point
 * Description: 'single digit numbers from zero to ten'
 * Return: Always 0
 */
int main(void)
{
	int n;

	for (n = 0 ; n < 10 ; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
